name: homelab
description: Homelab containers managed with Pulumi
runtime: go
config:
  pulumi:tags:
    value:
      pulumi:template: go

# Import environment configuration from Pulumi ESC
environment:
  - homelab-dev
