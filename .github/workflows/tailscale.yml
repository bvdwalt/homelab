on:
  workflow_dispatch:
  workflow_call:

jobs:
  tailscale_connect:
    name: Tailscale Connect
    runs-on: ubuntu-24.04
    steps:
      - name: Tailscale
        uses: tailscale/github-action@v4
        with:
          ping: ${{ secrets.DOCKER_HOSTNAME }}
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci
