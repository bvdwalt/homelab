apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |-
    priorityClassName: system-cluster-critical
    providers:
      kubernetesCRD:
        allowCrossNamespace: true
    ports:
      web:
        redirections:
          entryPoint:
            to: websecure
            scheme: https
            permanent: true
    logs:
      access:
        enabled: true
        format: json
    experimental:
      plugins:
        traefik-oidc-auth:
          moduleName: github.com/sevensolutions/traefik-oidc-auth
          version: v0.12.0
        crowdsec-bouncer:
          moduleName: github.com/maxlerebourg/crowdsec-bouncer-traefik-plugin
          version: v1.4.5
    additionalArguments:
      - "--entrypoints.websecure.http.middlewares=traefik-config-crowdsec-bouncer@kubernetescrd,traefik-config-nice-404@kubernetescrd,traefik-config-maintenance-page-errors@kubernetescrd"
    deployment:
      additionalVolumes:
        - name: plugins
          hostPath:
            path: /var/lib/traefik-plugins
            type: DirectoryOrCreate
      initContainers:
        - name: wait-for-dns
          image: busybox:1.38@sha256:cac8f90bbee42dc962a6b38bb1a235948d070385bb9d996bba15a6db8d364008
          command:
            - sh
            - -c
            - until nslookup kubernetes.default.svc.cluster.local; do echo waiting for cluster DNS; sleep 2; done
        - name: plugins-permissions
          image: busybox:1.38@sha256:cac8f90bbee42dc962a6b38bb1a235948d070385bb9d996bba15a6db8d364008
          command:
            - sh
            - -c
            - chown -R 65532:65532 /plugins-storage
          securityContext:
            runAsUser: 0
            runAsNonRoot: false
          volumeMounts:
            - name: plugins
              mountPath: /plugins-storage
    additionalVolumeMounts:
      - name: plugins
        mountPath: /plugins-storage
