apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |-
    priorityClassName: system-cluster-critical
    providers:
      kubernetesCRD:
        allowCrossNamespace: true
    ports:
      web:
        redirections:
          entryPoint:
            to: websecure
            scheme: https
            permanent: true
    logs:
      access:
        enabled: true
        format: json
    experimental:
      plugins:
        traefik-oidc-auth:
          moduleName: github.com/sevensolutions/traefik-oidc-auth
          version: v0.12.0
        crowdsec-bouncer:
          moduleName: github.com/maxlerebourg/crowdsec-bouncer-traefik-plugin
          version: v1.4.5
    additionalArguments:
      - "--entrypoints.websecure.http.middlewares=kube-system-crowdsec-bouncer@kubernetescrd,kube-system-nice-404@kubernetescrd,kube-system-maintenance-page-errors@kubernetescrd"
    deployment:
      additionalVolumes:
        - name: plugins
          hostPath:
            path: /var/lib/traefik-plugins
            type: DirectoryOrCreate
      initContainers:
        - name: plugins-permissions
          image: busybox:1.36@sha256:73aaf090f3d85aa34ee199857f03fa3a95c8ede2ffd4cc2cdb5b94e566b11662
          command:
            - sh
            - -c
            - chown -R 65532:65532 /plugins-storage
          securityContext:
            runAsUser: 0
            runAsNonRoot: false
          volumeMounts:
            - name: plugins
              mountPath: /plugins-storage
    additionalVolumeMounts:
      - name: plugins
        mountPath: /plugins-storage
