apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: household-oidc-auth
  namespace: kube-system
spec:
  plugin:
    traefik-oidc-auth:
      Provider:
        Url: "https://pocketid.greedo.net"
        ClientId: "${HOUSEHOLD_AUTH_CLIENT_ID}"
        ClientSecret: "${HOUSEHOLD_AUTH_CLIENT_SECRET}"
      Scopes: ["openid", "profile", "email"]
      Secret: "${HOUSEHOLD_OIDC_SESSION_SECRET}"
      BypassAuthenticationRule: "Header(`X-Oidc-Bypass-Key`, `${OIDC_BYPASS_HEADER_SECRET}`)"
      HeaderPassthrough:
        - "Remote-User=preferred_username"
        - "Remote-Name=name"
        - "Remote-Email=email"
