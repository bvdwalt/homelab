apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: app-actual-budget
  namespace: flux-system
spec:
  interval: 10m
  path: ./k8s/manifests/actual-budget
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: infrastructure-configs
